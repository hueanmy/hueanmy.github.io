---
interface Props {
  id: string;
  title: string;
  description: string;
  tagline?: string;
  thumbnail?: string;
  duration?: string;
  tags?: string[];
  featured?: boolean;
  paid?: boolean;
  price?: string;
}

const {
  id,
  title,
  description,
  tagline,
  thumbnail,
  duration,
  tags = [],
  featured,
  paid,
  price,
} = Astro.props;

const priceLabel = paid ? (price ?? 'Paid') : null;

const href = `/courses/${id}/`;
---

<a class={`card course${featured ? ' featured' : ''}`} href={href}>
  {
    thumbnail && (
      <div class="thumb">
        <img src={thumbnail} alt={title} loading="lazy" />
        {priceLabel && <span class="price-badge">{priceLabel}</span>}
        {duration && <span class="duration">{duration}</span>}
      </div>
    )
  }
  {!thumbnail && priceLabel && <span class="price-badge inline">{priceLabel}</span>}
  <h3>{title}</h3>
  {tagline && <p class="tagline">{tagline}</p>}
  <p>{description}</p>
  {
    tags.length > 0 && (
      <div class="tech">
        {tags.map((t) => (
          <span class="tag">{t}</span>
        ))}
      </div>
    )
  }
</a>

<style>
  a.card.course {
    display: flex;
    flex-direction: column;
    color: inherit;
    padding: 0.9rem;
    gap: 0.4rem;
  }
  .thumb {
    position: relative;
    aspect-ratio: 16 / 10;
    max-height: 130px;
    margin: -0.15rem -0.15rem 0.3rem;
    border-radius: 6px;
    overflow: hidden;
    background: #000;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .thumb img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }
  .duration {
    position: absolute;
    bottom: 0.4rem;
    right: 0.4rem;
    background: rgba(7, 9, 14, 0.85);
    color: var(--fg);
    font-family: var(--font-mono);
    font-size: 0.68rem;
    padding: 0.1rem 0.4rem;
    border-radius: 4px;
  }
  .price-badge {
    position: absolute;
    top: 0.4rem;
    right: 0.4rem;
    background: var(--gradient-accent);
    color: #07090e;
    font-family: var(--font-mono);
    font-size: 0.68rem;
    font-weight: 600;
    padding: 0.15rem 0.5rem;
    border-radius: 4px;
    letter-spacing: 0.02em;
    box-shadow: 0 2px 8px rgba(94, 234, 212, 0.25);
  }
  .price-badge.inline {
    position: static;
    align-self: flex-start;
    margin-bottom: 0.25rem;
  }
  a.card.course :global(h3) {
    font-size: 0.95rem;
    margin: 0;
  }
  .tagline {
    color: var(--accent);
    font-size: 0.7rem;
    font-family: var(--font-mono);
    letter-spacing: 0.08em;
    margin: 0;
  }
  a.card.course :global(p) {
    font-size: 0.82rem;
    line-height: 1.5;
    margin: 0;
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  .tech {
    display: flex;
    flex-wrap: wrap;
    gap: 0.3rem;
    margin-top: auto;
    padding-top: 0.5rem;
  }
  .tech :global(.tag) {
    font-size: 0.66rem;
    padding: 0.1rem 0.45rem;
  }
  .card.featured {
    border-color: rgba(94, 234, 212, 0.35);
  }
</style>
