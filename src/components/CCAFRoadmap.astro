---
const meta = [
  { k: '60', v: 'câu' },
  { k: '120', v: 'phút' },
  { k: '720', v: '/1000 pass' },
  { k: '$99', v: 'phí thi' },
];

const cards = [
  {
    href: '/cca-f-roadmap.html',
    badge: '30h · 4-week plan',
    title: 'Study Roadmap',
    tagline: 'Lộ trình ôn 30h chia 4 tuần — 5 domains, 6 exam scenarios, common traps và mental models.',
    bullets: [
      ['①', 'Foundation · Agentic', '8h'],
      ['②', 'Tools & Claude Code', '10h'],
      ['③', 'Prompt & Context', '8h'],
      ['④', 'Practice scenarios', '4h'],
    ],
    cta: 'View roadmap →',
  },
  {
    href: '/cca-f-knowledge-map.html',
    badge: 'Per-domain reference',
    title: 'Knowledge Map',
    tagline: 'Deep-dive từng domain — knowledge items K1…Kn, code snippets, traps & refs. Tra cứu nhanh.',
    bullets: [
      ['K', '35+ knowledge items'],
      ['◆', 'Code snippets per concept'],
      ['◆', 'Sidebar nav by domain'],
      ['◆', 'Traps + models inline'],
    ],
    cta: 'View knowledge map →',
  },
  {
    href: '/cca-f-vocabulary.html',
    badge: 'EN ↔ VI glossary',
    title: 'Vocabulary Guide',
    tagline: 'Từ vựng tiếng Anh hay gặp trong đề thi — giải thích bằng tiếng Việt + ví dụ thực tế.',
    bullets: [
      ['A', '5 domain-aligned categories'],
      ['?', 'MCQ question phrases'],
      ['→', 'Search EN hoặc VI'],
      ['#', 'Tag: Core / Trap / Pattern'],
    ],
    cta: 'View vocabulary →',
  },
];
---

<section id="ccaf-exam" class="exam-section">
  <div class="section-head">
    <div class="head-left">
      <h2>CCA-F Exam</h2>
      <p class="head-sub">Claude Certified Architect — Foundations</p>
    </div>
    <div class="head-actions">
      <a class="head-link" href="https://github.com/hueanmy/cca-f-roadmap" target="_blank" rel="noopener">Working examples on GitHub ↗</a>
    </div>
  </div>

  <ul class="meta-pills">
    {meta.map((m) => (
      <li><strong>{m.k}</strong> {m.v}</li>
    ))}
    <li class="meta-soft">scenario-based, không phải trivia</li>
  </ul>

  <div class="exam-grid">
    {cards.map((c) => (
      <a class="exam-card" href={c.href} target="_blank" rel="noopener">
        <div class="exam-body">
          <span class="exam-badge">{c.badge}</span>
          <h3 class="exam-title">{c.title}</h3>
          <p class="exam-tagline">{c.tagline}</p>
          <ul class="bullets">
            {c.bullets.map((b) => (
              <li>
                <span class="bullet-mark">{b[0]}</span>
                <span class="bullet-text">{b[1]}</span>
                {b[2] && <span class="bullet-extra">{b[2]}</span>}
              </li>
            ))}
          </ul>
        </div>
        <div class="exam-footer">{c.cta}</div>
      </a>
    ))}
  </div>
</section>

<style>
  .exam-section { margin-top: 2.5rem; }

  .section-head {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 1rem;
    margin-bottom: 0.85rem;
    padding-bottom: 0.65rem;
    border-bottom: 1px solid var(--border);
  }
  .head-left h2 {
    font-family: var(--font-mono);
    font-size: 0.68rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.14em;
    color: var(--fg-muted);
    margin: 0 0 0.2rem;
  }
  .head-sub { font-size: 0.86rem; color: var(--fg); margin: 0; }
  .head-actions { display: flex; gap: 1.25rem; flex-wrap: wrap; justify-content: flex-end; padding-top: 0.15rem; }
  .head-link {
    font-family: var(--font-mono);
    font-size: 0.72rem;
    color: var(--accent);
    transition: color 120ms;
  }
  .head-link:hover { color: var(--accent-hover); text-decoration: underline; text-underline-offset: 3px; }

  .meta-pills {
    list-style: none;
    padding: 0;
    margin: 0 0 1.25rem;
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
  }
  .meta-pills li {
    font-family: var(--font-mono);
    font-size: 0.66rem;
    color: var(--fg-muted);
    border: 1px solid var(--border);
    border-radius: 20px;
    padding: 2px 9px;
  }
  .meta-pills strong { color: var(--fg); font-weight: 600; }
  .meta-pills li.meta-soft { color: var(--fg-dim); border-color: transparent; padding-left: 0; }

  .exam-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1rem;
  }

  .exam-card {
    display: flex;
    flex-direction: column;
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    color: inherit;
    text-decoration: none;
    transition: box-shadow 150ms ease;
    overflow: hidden;
  }
  .exam-card:hover { box-shadow: 0 4px 20px rgba(0,0,0,0.07); }

  .exam-body {
    flex: 1;
    padding: 1rem 1rem 0.75rem;
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
  }

  .exam-badge {
    display: inline-block;
    align-self: flex-start;
    font-family: var(--font-mono);
    font-size: 0.6rem;
    color: var(--accent);
    border: 1px solid var(--accent);
    padding: 2px 8px;
    border-radius: 20px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    opacity: 0.8;
  }

  .exam-title {
    font-family: var(--font-serif);
    font-size: 1.0625rem;
    font-weight: 500;
    color: var(--fg);
    line-height: 1.3;
    margin: 0;
  }

  .exam-tagline {
    font-size: 0.8rem;
    color: var(--fg-dim);
    line-height: 1.5;
    margin: 0;
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .bullets {
    list-style: none;
    padding: 0;
    margin: 0.25rem 0 0;
    display: flex;
    flex-direction: column;
    gap: 0.15rem;
    flex: 1;
  }
  .bullets li {
    display: grid;
    grid-template-columns: 1.2rem 1fr auto;
    align-items: center;
    gap: 0.4rem;
    font-size: 0.74rem;
    color: var(--fg);
    padding: 0.12rem 0;
  }
  .bullet-mark {
    font-family: var(--font-mono);
    font-size: 0.68rem;
    color: var(--accent);
    text-align: center;
  }
  .bullet-extra {
    font-family: var(--font-mono);
    font-size: 0.62rem;
    color: var(--fg-muted);
  }

  .exam-footer {
    border-top: 1px solid var(--border);
    padding: 0.65rem 1rem;
    font-family: var(--font-mono);
    font-size: 0.72rem;
    color: var(--accent);
    transition: color 120ms;
  }
  .exam-card:hover .exam-footer { color: var(--accent-hover); }

  @media (max-width: 720px) {
    .exam-grid { grid-template-columns: 1fr 1fr; }
    .exam-grid > .exam-card:nth-child(3) { grid-column: 1 / -1; }
  }
  @media (max-width: 480px) {
    .exam-grid { grid-template-columns: 1fr; }
    .exam-grid > .exam-card:nth-child(3) { grid-column: auto; }
  }
  @media (max-width: 520px) {
    .section-head { flex-direction: column; gap: 0.5rem; }
    .head-actions { justify-content: flex-start; }
  }
</style>
